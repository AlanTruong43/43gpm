========================================
    43GPM PROFILE DASHBOARD
    Hướng dẫn sử dụng
========================================

📋 YÊU CẦU:
-----------
1. Python 3.8 trở lên đã được cài đặt
2. 43GPM phải đang chạy trên port 19995
3. Các dependencies đã được cài đặt (đã có sẵn)


🚀 CÁCH CHẠY DASHBOARD:
-----------------------

Cách 1: Double-click file (ĐƠN GIẢN NHẤT) ⭐
   → Double-click vào file: START_DASHBOARD.bat
   → Dashboard sẽ tự động mở trong browser

Cách 2: Dùng lệnh
   → Mở PowerShell/CMD trong thư mục project
   → Chạy lệnh: python -m streamlit run dashboard.py


🌐 TRUY CẬP:
------------
Dashboard sẽ tự động mở tại:
   → http://localhost:8501


📱 CÁCH SỬ DỤNG DASHBOARD:
--------------------------

1. TAB PROFILES:
   - Xem danh sách tất cả profiles
   - Tìm kiếm và lọc profiles
   - Mở/đóng profiles
   - Xem thông tin chi tiết

2. TAB RUN PROJECT:
   - Chọn project muốn chạy (twitter.py, instagram.py, etc.)
   - Chọn profile đã mở
   - Click "Run Project" để chạy automation

3. TAB STATUS:
   - Xem profiles đang chạy
   - Theo dõi debug address
   - Đóng profiles khi hoàn thành


📁 TẠO PROJECT MỚI:
-------------------

1. Tạo file .py mới trong folder "project/"
   Ví dụ: project/instagram.py

2. File phải có function run(profile_data):

   def run(profile_data):
       from selenium import webdriver
       from selenium.webdriver.chrome.options import Options
       
       chrome_options = Options()
       chrome_options.add_experimental_option(
           "debuggerAddress", 
           profile_data['remote_debugging_address']
       )
       
       driver = webdriver.Chrome(options=chrome_options)
       
       # Code automation của bạn ở đây
       driver.get("https://instagram.com")
       
       # KHÔNG gọi driver.quit()
       print("Hoàn thành!")

3. Dashboard sẽ tự động nhận diện project mới


⚠️ LƯU Ý QUAN TRỌNG:
--------------------
- 43GPM PHẢI đang chạy trước khi mở dashboard
- Mở profile TRƯỚC KHI chạy project
- KHÔNG gọi driver.quit() trong code automation
- Để dashboard quản lý việc đóng browser


🐛 KHI GẶP LỖI:
---------------

Lỗi: "Cannot connect to 43GPM"
   → Kiểm tra 43GPM có đang chạy không
   → Kiểm tra port 19995 có bị block không
   → Thử truy cập: http://127.0.0.1:19995/api/v3/groups

Lỗi: "streamlit is not recognized"
   → Dùng lệnh: python -m streamlit run dashboard.py
   → Hoặc dùng file START_DASHBOARD.bat

Lỗi: Profile không mở được
   → Kiểm tra profile có tồn tại trong 43GPM không
   → Restart 43GPM và thử lại
   → Kiểm tra profile có đang mở bởi process khác không


💡 TIPS:
--------
- Có thể mở nhiều profiles cùng lúc
- Dashboard tự động reload khi thay đổi code
- Nhấn Ctrl+C để dừng dashboard
- Dùng filter và search để tìm profiles nhanh


📞 HỖ TRỢ:
----------
Nếu cần hỗ trợ, check lại:
1. ✅ 43GPM đang chạy
2. ✅ Python dependencies đã cài
3. ✅ Profile đã mở trước khi run project
4. ✅ Function run(profile_data) có trong file project


========================================
    CHÚC BẠN SỬ DỤNG THÀNH CÔNG! 🎉
========================================

